spring:
  application:
    name: config-client

  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:3990/
      bus:
        enabled: true
        trace:
          enabled: true

server:
  port: 3991



management:
  endpoints:
    web:
      exposure:
        include: bus-refresh